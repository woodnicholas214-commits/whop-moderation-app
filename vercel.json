{
  "buildCommand": "prisma generate && DATABASE_URL=\"$POSTGRES_URL_NON_POOLING\" prisma migrate deploy && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}

